% =====================================================================
% main.tex — SOCC 2026 paper, Stage 1 skeleton
%
% TLG-mapped BNN tile in a fully-digital sky130 SoC built with the
% open-flow (LibreLane + OpenRAM + PicoRV32). 6-page IEEE conference
% format, ~3,500 words body, 4 figures, 2 tables, ~28 references.
%
% Build:  make           (pdflatex + bibtex + pdflatex x2)
% Clean:  make clean
% =====================================================================

\documentclass[conference]{IEEEtran}

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{url}
\usepackage[font=small,labelfont=bf,skip=2pt]{caption}
\usepackage[skip=1pt]{subcaption}

% Repo-local figure search path so \includegraphics{figX_*} resolves.
\graphicspath{{figures/}}

% XNOR operator (binary-inner-product idiom under {-1,+1} -> {0,1}
% encoding) — used in Sections I and III-B.
\newcommand{\xnor}{\mathbin{\overline{\oplus}}}

\begin{document}

% ---------------------------------------------------------------------
% Title and authors
% ---------------------------------------------------------------------
\title{TINKER: A Fully-Digital Threshold-Logic-Mapped\\
  BNN Inference SoC}

\author{\IEEEauthorblockN{Abdullah Sahruri and Martin Margala}
  \IEEEauthorblockA{School of Computing and Informatics\\
    University of Louisiana at Lafayette\\
    Lafayette, LA, USA\\
    \{abdullah.sahruri, martin.margala\}@louisiana.edu}}

\maketitle

% ---------------------------------------------------------------------
% Abstract — fill last (Stage 7).
% ---------------------------------------------------------------------
\begin{abstract}
  Threshold logic --- implementing neural-network-style inner
  products as gates that fire when weighted inputs exceed a
  threshold --- has a long architectural lineage, but its silicon
  implementations have historically required exotic devices: flash
  transistors, memristors, or current-mode analog cells. We show
  that the threshold function decomposes cleanly onto ordinary
  digital standard cells via a chunked-LUT scheme, eliminating the
  dependency on fabrication-specific primitives that has constrained
  threshold-logic accelerators. We demonstrate this at SoC scale
  with TINKER, a sixteen-neuron threshold-logic-mapped tile
  integrated with a PicoRV32 RV32I core, a Wishbone B4 fabric, and
  OpenRAM IMEM/DMEM macros, hosting an entire grouped binary MLP
  through five software-scheduled sequential evaluations. On the
  full 10\,000-image MNIST test set, TINKER reaches 85.60\,\%
  accuracy with a 95\,\% Wilson confidence interval of
  [84.90\,\%, 86.27\,\%], at 8.06\,\textmu J per inference and
  47.21\,mW activity-aware power. Hierarchical activity-aware
  decomposition shows that the threshold-logic compute occupies
  24.3\,\% of total SoC power --- the largest active datapath
  bucket in the system --- validating fully-digital threshold logic
  as a viable primary inference compute element at SoC scale, not
  merely a curiosity confined to specialized fabrication flows.
  TINKER is implemented on the open sky130 flow and is
  sign-off-clean on KLayout DRC, Netgen LVS, KLayout XOR, and
  antenna.
\end{abstract}

\begin{IEEEkeywords}
  % TODO(stage 7): pick 4--6 IEEE keywords.
  binary neural networks, threshold logic, system-on-chip, RISC-V, MNIST inference.
\end{IEEEkeywords}

% =====================================================================
% I. Introduction — Target: ~500 words / 0.75 page
% ---------------------------------------------------------------------
% Motivate: ML inference on open-flow ASICs is a stress test for the
% open toolchain; BNNs are the right workload (single-bit math removes
% multiplier-area and quantization-aware-training overhead); the open
% problem is that prior open-flow demos either skip the SoC layer or
% skip the BNN. List the five contributions verbatim from the brief.
% Close with a roadmap of the remaining sections.
% =====================================================================
\section{Introduction}
\label{sec:intro}

Threshold logic --- gates that fire when a weighted sum of inputs
crosses a programmed threshold --- has a long architectural lineage
as a substrate for neural-network-style inner products and dense
combinational computation~\cite{beiu2003vlsi}. Its silicon
implementations, however, have historically required exotic
devices. Memristor-based threshold gates exploit non-volatile
resistance for weight storage and current-mode summation
~\cite{papandroulidakis2019practical}; differential and
current-mode analog approaches use precise current sources to
realise the weighted sum~\cite{beiu2003differential}; the most
recent ASIC in this lineage uses programmable flash devices for
the threshold compare~\cite{tulip}. Each implementation ties the
design to a specific fabrication flow. Whether threshold logic
can be carried by ordinary digital standard cells, without exotic
primitives, at SoC scale is the architectural question this work
answers.

Our answer is a chunked-LUT decomposition of the threshold
function that maps onto \texttt{sky130\_fd\_sc\_hd} standard cells
--- only multiplexers, AOI cells, and a conventional adder tree,
with no analog or mixed-signal devices. We built TINKER, a
sixteen-neuron threshold-logic-mapped BNN inference SoC, to
demonstrate this end-to-end. The threshold-logic tile is
integrated with a PicoRV32~\cite{picorv32} RV32I core over a
Wishbone B4 fabric, alongside OpenRAM IMEM/DMEM macros and a
sim-observability GPIO. The deployed network is a grouped binary
MLP over a 14\,\(\times\)\,14 input: four parallel
49\,\(\to\,16\) branches concatenate into a 64-bit hidden vector
that the same tile maps to ten output logits in a fifth
evaluation, with all five tile passes scheduled by software.
Off-chip training uses a hard-tanh straight-through
estimator~\cite{hubara_bnn} and a OneCycleLR
schedule~\cite{onecycle}; the open RTL-to-GDSII pipeline is
LibreLane~\cite{librelane} on sky130A~\cite{sky130_pdk} with
OpenROAD~\cite{openroad}, Yosys~\cite{yosys}, and
OpenRAM~\cite{openram} macros.

The contributions of TINKER are threefold. First, we show that
threshold logic does not require exotic devices: a chunked-LUT
decomposition of the XNOR-popcount-threshold core lowers the
threshold function onto \texttt{sky130\_fd\_sc\_hd} multiplexers
and AOI cells, breaking the dependency on flash, memristor, or
current-mode primitives that has historically constrained
threshold-logic accelerators~\cite{tulip,
papandroulidakis2019practical, beiu2003differential}. The
standalone TLG tile synthesises to 7\,184 standard cells and
408 sequential elements within 0.116\,mm$^2$. Second, we
demonstrate threshold logic at SoC scale: the TLG tile is
integrated with a PicoRV32 RV32I core over a Wishbone B4 fabric
with OpenRAM IMEM/DMEM macros, with an entire grouped binary MLP
hosted on the same tile RTL through five software-scheduled
sequential evaluations --- no per-layer hardware specialisation.
Third, we discover that fully-digital threshold-logic compute is
the dominant active power bucket in the system that demonstrates
it: with OpenRAM macros architecting the memory subsystem, the
TLG compute occupies 24.3\,\% of total SoC power
(vs 10.81\,\% under a register-file baseline at byte-identical
tile RTL), validating threshold logic as a primary inference
compute element rather than a peripheral curiosity. End-to-end
MNIST inference reaches 85.60\,\% accuracy with a 95\,\% Wilson
confidence interval of \([84.90\,\%,\,86.27\,\%]\) on the full
10\,000-image test set, at 8.06\,\textmu J per image.

Section~\ref{sec:background} surveys the threshold-logic
architectural lineage and digital BNN accelerators.
Section~\ref{sec:arch} describes the SoC architecture and the
TLG decomposition. Section~\ref{sec:impl} covers implementation,
sign-off, and BNN deployment. Section~\ref{sec:eval} presents
evaluation. Section~\ref{sec:discussion} discusses findings, and
Section~\ref{sec:conclusion} concludes.

% =====================================================================
% II. Background and Related Work — Target: ~350 words / 0.5 page
% ---------------------------------------------------------------------
% Three short paragraphs:
%   (a) digital BNN accelerators — XNE, YodaNN, ChewBaccaNN, FBNA,
%       FINN — and what each leaves on the table for an open-flow SoC;
%   (b) open-source ASIC infrastructure — sky130, OpenLane/LibreLane,
%       OpenRAM, PicoRV32 — and the small set of prior tape-outs that
%       combined them (OpenSpike is the closest cousin);
%   (c) one paragraph framing where this work sits — fully-digital
%       TLG mapping in sky130 std cells, BNN payload, full SoC.
% =====================================================================
\section{Background and Related Work}
\label{sec:background}

Threshold logic, in the canonical sense surveyed
by~\cite{beiu2003vlsi}, realises a Boolean function whose output
fires when a weighted sum of inputs crosses a programmed threshold
--- a primitive with a long architectural history as a substrate
for dense combinational and neural-network-style computation.
Silicon implementations of the threshold function span several
device families. Differential and current-mode analog approaches
use precise current sources to realise the weighted
sum~\cite{beiu2003differential}. Memristor-based threshold gates
exploit non-volatile resistance for weight storage and
current-mode summation~\cite{papandroulidakis2019practical}.
Most recently, programmable threshold-logic standard cells with
embedded flash for the threshold compare push the lineage into
the ASIC regime~\cite{tulip}. The unifying constraint across these
implementations is fabrication binding: each ties the design to a
specific PDK family by its choice of threshold-compare primitive,
and none carries threshold logic on ordinary digital standard
cells. TINKER is, to our knowledge, the first fully-digital TLG
implementation at SoC scale: a chunked-LUT decomposition of the
threshold function lowers entirely onto
\texttt{sky130\_fd\_sc\_hd} multiplexers and AOI cells, with no
exotic primitives.

The digital BNN accelerator landscape provides the second
reference frame. YodaNN~\cite{yodann2018} pairs a binary-weight
datapath with standard-cell memories and aggressive voltage
scaling in UMC 65\,nm. XNE~\cite{xne2018} integrates an
XNOR-popcount engine with a hybrid SRAM/SCM memory subsystem
alongside a microcontroller in 22\,nm.
ChewBaccaNN~\cite{chewbaccann2021} couples a flexible
XNOR-and-popcount-tree datapath with body-biasing and 0.4\,V
operation in GF 22\,nm FDX. These accelerators implement the BNN
inner product using conventional adder trees and comparators,
not threshold-logic decomposition. At the system level,
OpenSpike~\cite{openspike} is the closest open-flow SoC
precedent on sky130 (PicoRV32 + OpenRAM) but targets a spiking
rather than a binary network. FPGA-side BNN frameworks such as
FINN~\cite{finn} occupy a separate design point not directly
compared. With the threshold-logic lineage and BNN landscape
established, the following sections describe the architecture
(Section~\ref{sec:arch}), implementation (Section~\ref{sec:impl}),
and evaluation (Section~\ref{sec:eval}) of TINKER.

% =====================================================================
% III. SoC Architecture — Target: ~900 words / 1.5 page, 2 figures
% ---------------------------------------------------------------------
% Subsections:
%   III-A System overview (Fig. 1: SoC block diagram). PicoRV32 +
%         Wishbone B4 single-master + 4 slaves (IMEM, DMEM, tile,
%         GPIO). Memory map. Clock/reset.
%   III-B Tile microarchitecture (Fig. 2). 16 neurons x 64-bit
%         XNOR-popcount with per-neuron 7-bit threshold; loadable
%         weights via cfg regfile.
%   III-C Tile wrapper protocol — two-phase 64-bit weight write,
%         polled BUSY/DONE handshake, drop-on-busy semantics.
% Source: docs/PHASE3_ARCHITECTURE.md sections 1, 4, 8.
% =====================================================================
\section{SoC Architecture}
\label{sec:arch}

\begin{figure*}[t]
  \centering
  \begin{subfigure}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{fig1_soc_block.pdf}
    \caption{SoC architecture block diagram.}
    \label{fig:soc-block}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=1\linewidth]{fig2_tile_microarch.pdf}
    \caption{TLG-mapped BNN tile microarchitecture.}
    \label{fig:tile-microarch}
  \end{subfigure}
  \caption{Open-flow BNN inference SoC.
    \subref{fig:soc-block}~PicoRV32 master + Wishbone B4 + OpenRAM
    IMEM/DMEM + TLG-mapped tile + GPIO.
    \subref{fig:tile-microarch}~$\times 16$ XNOR-popcount-threshold
    datapath with eleven-chunk TLG-decomposed popcount.}
  \label{fig:overview}
\end{figure*}

\subsection{System overview}
\label{sec:arch:overview}

TINKER is a single-clock RV32I system whose role is to host and
orchestrate the threshold-logic tile under software control. A
PicoRV32 core~\cite{picorv32} acts as the lone Wishbone B4 classic
master and drives a single-cycle bus with 32-bit address and
32-bit data; address decode on \texttt{adr[31:28]} dispatches to
one of four word-aligned slaves --- IMEM at
\texttt{0x0000\_0000}, DMEM at \texttt{0x1000\_0000}, the tile
peripheral at \texttt{0x2000\_0000}, and a sim-observability
GPIO at \texttt{0x3000\_0000}. Reset is asynchronously asserted
and synchronously deasserted through a two-stage flop chain;
firmware boots bare-metal from \texttt{PROGADDR\_RESET\,=\,0}.
A single-master, four-slave decode is sufficient for the polled
inference handshake; an interrupt controller, MMIO timer, and
DMA would be dead area for this workload. The system clock is
nominally 100\,MHz, matching the SDC inherited from the standalone
tile characterisation. Fig.~\ref{fig:soc-block} shows the block
diagram.

\subsection{Threshold-logic decomposition and tile microarchitecture}
\label{sec:arch:tile}

\paragraph{The threshold function.} Each tile neuron computes the
binary inner product
\(y_i = \mathbb{1}\!\left[\,\mathrm{popcount}\!\left(x \xnor w_i\right) \ge \theta_i\,\right]\),
with \(x \in \{-1,+1\}^{64}\) and per-neuron 64-bit weight \(w_i\)
and 7-bit threshold \(\theta_i\). This is the threshold function in
the canonical sense of~\cite{beiu2003vlsi}: a Boolean output that
fires when a weighted sum of binary inputs crosses a programmed
threshold. Sixteen such neurons share the registered input and
produce a 16-bit output vector per evaluation, with two-cycle
latency. The challenge for a fully-digital realisation is that a
naive direct mapping of the threshold function to a single gate
requires precise summation of weighted inputs --- which is what
the current-mode~\cite{beiu2003differential} and
memristor-based~\cite{papandroulidakis2019practical} implementations
do natively but the digital domain does not.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig_tlg_decomp.pdf}
  \caption{Chunked-LUT decomposition of the 64-bit threshold
  function. The XNOR output partitions into ten 6-bit chunks and
  one 4-bit residue; each chunk's population count is synthesised
  by Yosys into a network of \texttt{sky130\_fd\_sc\_hd}
  multiplexers and AOI cells (red). The eleven 3-bit subcounts
  feed a conventional adder tree and threshold comparator (navy).
  The entire decomposition uses only digital standard cells, with
  no analog or mixed-signal primitives.}
  \label{fig:tlg-decomp}
\end{figure}

\paragraph{Chunked-LUT decomposition.} Our decomposition recasts
the 64-input threshold function as a small adder-tree of population
counts over fixed-size chunks. The 64-bit XNOR output is partitioned
into eleven chunks --- ten 6-bit chunks plus one 4-bit residue.
Each 6-bit chunk's population count is a small Boolean function
($6 \to 3$) that Yosys synthesises into a network of
\texttt{sky130\_fd\_sc\_hd} multiplexers and AOI cells; the 4-bit
residue ($4 \to 3$) is similarly decomposed. The eleven 3-bit
subcounts feed a conventional width-7 adder tree, and the 7-bit
sum is compared against \(\theta_i\) by a standard magnitude
comparator. Fig.~\ref{fig:tlg-decomp} visualises the decomposition.
The chunked approach is the design point that makes digital TLG
practical: a single 64-input LUT would be prohibitively large; one
threshold gate per input bit would explode in fanin and demand
analog summation. The 6-bit chunk size matches
\texttt{sky130\_fd\_sc\_hd}'s mux/AOI cost --- large enough that
the eleven-way partitioning overhead is acceptable, small enough
that each chunk LUT fits comfortably as a combinational cell
network.

\paragraph{Quantification and contrast.} The standalone TLG tile
synthesises to 7\,184 standard cells (408 sequential elements, the
remainder combinational logic across the eleven chunk LUTs, the
adder tree, and the configuration regfile) within 0.116\,mm$^2$;
within the integrated SoC build the tile bucket is 6\,519 cells
(slight variation from cross-boundary optimisation). The principal
contrast with TULIP~\cite{tulip} is the threshold-compare primitive:
TULIP uses embedded flash devices, tying the design to a specific
PDK family; memristor-based~\cite{papandroulidakis2019practical}
and current-mode~\cite{beiu2003differential} TLG face the same
PDK-binding constraint. Because every chunk LUT and the
surrounding adder tree resolve to digital standard cells, TINKER's
RTL ports unmodified to any digital PDK with a comparable cell
library. The configuration regfile holds 16 weight words (64-bit)
and 16 thresholds (7-bit), addressed through a 6-bit
\texttt{cfg\_addr}; weights are loaded once per network and the cfg
port carries traffic only at boot. An \texttt{x\_valid} strobe
latches the input register and starts the two-cycle pipeline;
\texttt{y\_valid} pulses on the cycle the output leaves.

\subsection{Tile wrapper and inference handshake}
\label{sec:arch:wrapper}

The Wishbone-to-tile bridge \texttt{wb\_tile\_wrapper} occupies a
512-byte slave window and exposes the tile's cfg port and
\(x_\text{in}/y_\text{out}\) handshake as memory-mapped registers.
Weights and inputs are committed by two-phase 32-bit writes
(\texttt{LO} then \texttt{HI}), with the \texttt{HI} write
assembling the 64-bit value and pulsing the corresponding strobe;
thresholds are atomic single-word writes. Firmware polls
\texttt{STATUS.BUSY} until clear, reads \texttt{YOUT}, and writes
\texttt{CTRL.CLR\_DONE} to arm the next inference. Writes to
weight, threshold, or input registers while \texttt{BUSY\,=\,1}
are silently dropped: the bus access acks normally but no shadow
is updated, causing a misbehaving program to hang visibly rather
than corrupt an in-flight evaluation. This is a deliberate
testability choice: a silently corrupted inference is a far worse
failure mode than an observably hung firmware loop. The wrapper
carries roughly 1.3\,K flops, ~700 of them purely for read-back of
every writable register.

The memory subsystem instantiates two pre-hardened
OpenRAM~\cite{openram} macros
(\texttt{sky130\_sram\_2kbyte\_1rw1r\_32x512\_8} for the IMEM,
\texttt{sky130\_sram\_1kbyte\_1rw1r\_32x256\_8} for the DMEM),
wrapped by a small Wishbone FSM that inserts one wait state on
reads. The motivation for OpenRAM macros over a register-file
baseline is power; the system-level consequence is detailed in
Section~\ref{sec:eval:power}.

% =====================================================================
% IV. Implementation — Target: ~550 words / 1.0 page, 1 figure (Fig. 3)
% ---------------------------------------------------------------------
% Subsections:
%   IV-A Open-flow toolchain — LibreLane 3.0.3, sky130A PDK,
%        OpenRAM macros (sky130_sram_2kbyte_1rw1r_32x512_8 +
%        sky130_sram_1kbyte_1rw1r_32x256_8). One sentence on the
%        TT-only macro Liberty caveat (cite §V).
%   IV-B Memory integration — wait-state FSM in wb_imem/wb_dmem,
%        dout0_q latch fix in one sentence, sim-only preload via
%        ifdef __ICARUS__.
%   IV-C Firmware — PicoRV32 bare-metal C, 14x14 grouped BNN
%        inference loop, 8 words/image DMEM layout.
%   IV-D Layout (Fig. 3: annotated GDS) — die 2.25 mm^2 (std-cell
%        0.380 + macro 0.475), 0 errors on KLayout DRC / Netgen
%        LVS / antenna. One sentence on Magic-DRC false positives.
% Source: docs/PHASE3_5_NOTES.md sections 2, 3, 8 + PHASE4_5_NOTES.md.
% =====================================================================
\section{Implementation}
\label{sec:impl}

\subsection{Open-flow toolchain}
\label{sec:impl:flow}

The SoC is implemented through the LibreLane 3.0.3 RTL-to-GDSII
flow~\cite{librelane} on the SkyWater sky130A open-source
PDK~\cite{sky130_pdk}, in the OpenLane lineage~\cite{openlane}.
Yosys~\cite{yosys} synthesises the design against the
\texttt{sky130\_fd\_sc\_hd} standard-cell library;
OpenROAD~\cite{openroad} drives floorplanning, placement, clock-tree
synthesis, routing, and antenna repair; Magic and KLayout perform GDS
streamout and DRC; Netgen performs LVS. The two memories are
pre-hardened OpenRAM~\cite{openram} macros ---
\texttt{sky130\_sram\_2kbyte\_1rw1r\_32x512\_8}
(683\,\(\times\)\,417\,\textmu m) for the IMEM and
\texttt{sky130\_sram\_1kbyte\_1rw1r\_32x256\_8}
(480\,\(\times\)\,398\,\textmu m) for the DMEM --- manually placed
against a 1\,500\,\(\times\)\,1\,500\,\textmu m absolute floorplan with
PDN hookups to the sky130 dual rail (\texttt{vccd1}/\texttt{vssd1}).
Macro integration required iteration on placement constraints, an
OpenSTA blackbox stub for the behavioural macro views, and removal of
the wrapper-level \texttt{keep\_hierarchy} attributes to satisfy
LibreLane's flat-macro convention. The full RTL-to-GDSII pipeline
is driven from a single \texttt{config.json}, with multi-corner STA,
sign-off, and activity-aware power decomposition reproducible from
the same artifact.

\subsection{Sign-off methodology}
\label{sec:impl:signoff}
\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig3_layout.pdf}
  \caption{Post-P\&R floorplan of the open-flow BNN inference SoC on
    sky130A (1.5\,$\times$\,1.5\,mm$^2$ die). The IMEM and DMEM OpenRAM
    macros sit in the upper region; module footprints for the
    TLG-mapped tile, PicoRV32 core, Wishbone B4 fabric, and GPIO are
    extracted from the placed instance bounding boxes.}
  \label{fig:layout}
\end{figure}

Fig.~\ref{fig:layout} shows the post-P\&R floorplan, with the IMEM
and DMEM macros along the upper edge of the die and the std-cell logic
below. Multi-corner static timing was run across nine corners covering
the three sky130 process points (TT, FF, SS), three temperatures
($-40$\,\textdegree{}C, 25\,\textdegree{}C, 100\,\textdegree{}C), and
three supply voltages (1.60\,V, 1.80\,V, 1.95\,V). At the headline
\texttt{nom\_tt\_025C\_1v80} corner the design closes with
$+0.51$\,ns setup slack and $+0.37$\,ns hold slack; hold timing is
clean across all nine corners. The slow-corner setup violations at
\texttt{nom\_ss\_100C\_1v60} are an attribute of the OpenRAM macros'
single TT-corner Liberty --- a documented LibreLane 3.x limitation
discussed in Section~\ref{sec:discussion}, not a design defect.
Physical sign-off is clean on the four authoritative checks: KLayout
DRC reports 0 errors (the eight-million Magic DRC flags inside the
macro footprint are a known sky130 deficiency around hardened SRAM
internals and are conventionally ignored), Netgen LVS 0 errors,
KLayout XOR against the Magic-streamed GDS 0 differences across all
108 layers, and antenna 0 violating nets and 0 violating pins.

\subsection{BNN deployment}
\label{sec:impl:bnn}

With sign-off established, we describe the network deployed onto
the SoC. The deployed network is a grouped binary MLP over a
14\,\(\times\)\,14 input. After a 2\,\(\times\)\,2 average pool and a
per-quadrant median binarisation, four parallel branches each map a
49-bit quadrant to 16 hidden bits in one tile evaluation; the four
16-bit outputs concatenate into a 64-bit hidden vector that the same
tile maps to ten output logits in a fifth evaluation. Grouping
(rather than full $196 \to 64$ connectivity) is a hardware-shape
choice: each branch fits one tile-evaluation fanin, so the entire
network maps onto the SoC without changing tile or wrapper RTL.
The accuracy gap from full $196 \to 64$ connectivity to grouped
$4 \times (49 \to 16)$, measured against an unconstrained-fanin
training run on the same data with the same training recipe, is
0.87\,pp --- well below the run-to-run accuracy variance observed
across the three training seeds, and below the 0.69\,pp half-width
of the 95\,\% Wilson confidence interval on the deployed network's
MNIST accuracy.
Per-image inference therefore costs four hidden-layer batches plus
one output-layer batch, which the PicoRV32 firmware schedules
sequentially against the cfg-write/poll/read protocol of
Section~\ref{sec:arch:wrapper}, totalling 17\,068 cycles or
170.7\,\textmu s at 100\,MHz. The bare-metal firmware compiles to
roughly 1.5\,KB of RV32I and shares the IMEM with the binary weights;
per-image input bits are preloaded into DMEM by the testbench at
simulation time zero. A vectorised Python integer model serves as
the deployment golden, folding the trained batch-norm parameters into
per-neuron thresholds via
$t_i = \lceil (49 + \mathrm{sgn}(\gamma_i)\,\mu_i - \beta_i\sigma_i/|\gamma_i|)/2 \rceil$;
SoC RTL simulation matches this golden on all sixteen images of the
verification set. Network weights are trained off-chip with binary-weight
gradient descent~\cite{binaryconnect}, a hard-tanh
straight-through estimator~\cite{hubara_bnn}, a OneCycleLR
schedule~\cite{onecycle}, 30 epochs, and a three-seed sweep (seeds
42, 1042, 2042) with the best-validation seed deployed.

% =====================================================================
% V. Evaluation — Target: ~850 words / 1.5 page, Fig. 4 + Tables I, II
% ---------------------------------------------------------------------
% Subsections:
%   V-A Functional verification (Table I) — 4 gates: PyTorch <-> golden,
%       SoC sim <-> golden, full 10K MNIST eval, sign-off DRC/LVS.
%   V-B Power decomposition (Fig. 4) — hierarchical buckets at
%       max_ff_n40C_1v95: tile 24.3%, CPU 16.3%, IMEM 11.8%, DMEM
%       8.2%, clock 32.0%. The headline finding: tile fraction
%       jumped from 10.81% (regfile baseline) to 24.29% (OpenRAM).
%   V-C Energy and accuracy — 85.60% MNIST [84.90, 86.27] Wilson
%       95% CI, 8.06 uJ/image, 124,113 inferences/J at 100 MHz.
%   V-D Comparison (Table II) — vs XNE / YodaNN / ChewBaccaNN /
%       FBNA / OpenSpike. Be explicit about the technology and
%       open-flow vs commercial-flow distinction.
% Source: docs/PHASE3_5_NOTES.md sections 5--7 + PHASE4_5_NOTES.md
%         section 7 + results/phase3/comparison_baseline_vs_openram.md.
% =====================================================================
\section{Evaluation}
\label{sec:eval}

\subsection{Functional verification}
\label{sec:eval:verif}

The deployed network is exercised through four verification gates,
summarised in Table~\ref{tab:verification}. The vectorised Python
integer model \texttt{bnn\_reference.py} is treated as the deployment
golden, since its arithmetic is exact in the same fixed-width integer
domain as the on-chip XNOR-popcount; the trained PyTorch model is a
training-time proxy whose floating-point activations may differ near
batch-norm thresholds. The 16-image verification set is the first 16 images of the
MNIST test split, used as a fast inner-loop bit-exact check; the
full 10\,000-image test set is covered by the integer-golden gate.
PyTorch and the integer golden agree on 15 of these 16 images. The single mismatch (image~9: PyTorch labels
9, golden and SoC both label 7) lies on a folded-batch-norm activation
boundary near zero and is the expected behaviour of the integer
quantisation, not a bug. The post-P\&R SoC RTL simulation then matches
the integer golden bit-exact on all sixteen images. On the full
10\,000-image MNIST test set~\cite{mnist}, the same network reaches
85.60\,\% accuracy (8\,560 correct), with a 95\,\% Wilson confidence interval of
\([84.90\,\%,\,86.27\,\%]\); per-class accuracy ranges from 95.2\,\%
on digit~1 to 75.1\,\% on digit~5.
\input{tables/table1_verification}

\subsection{Activity-aware power decomposition}
\label{sec:eval:power}

Activity-aware power is reported at the headline
\texttt{max\_ff\_n40C\_1v95} corner. The full sixteen-image inference
testbench is replayed against the post-P\&R netlist, the chip-pin VCD
is annotated through OpenSTA's \texttt{read\_vcd}, supplemented with
forty-seven named-net activities for the major interior wires, and
\texttt{report\_power} is decomposed into per-instance buckets by
\texttt{scripts/bucket\_soc\_cells.py}. Total post-P\&R power is
47.21\,mW. Fig.~\ref{fig:power_breakdown} shows the hierarchical
decomposition. The clock tree is the largest single contributor at
15.10\,mW (32.0\,\%). The TLG-mapped tile is the largest active
datapath bucket at 11.47\,mW (24.3\,\%); the PicoRV32 core draws
7.71\,mW (16.3\,\%); the IMEM and DMEM macros draw 5.56\,mW
(11.8\,\%) and 3.85\,mW (8.2\,\%) respectively; shared logic and the
GPIO slave account for 1.24\,mW (2.6\,\%) and 0.30\,mW (0.6\,\%).
\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig4_power_breakdown.pdf}
  \caption{Hierarchical activity-aware power decomposition of the
    open-flow SoC at \texttt{max\_ff\_n40C\_1v95} (total 47.21\,mW).
    The TLG-mapped tile is the largest active datapath bucket once
    the OpenRAM macros remove register-file dominance from the
    memory subsystem.}
  \label{fig:power_breakdown}
\end{figure}

The actionable insight is the change in the tile's share of the
budget. With the register-file IMEM/DMEM of the Phase~3 baseline, the
same tile RTL --- byte-identical post-flatten netlist --- contributed
11.47\,mW of an SoC total of 106.06\,mW, or 10.81\,\%. Replacing the
register files with OpenRAM macros drops the system total to
47.21\,mW while leaving the tile bucket unchanged, lifting the tile's
share to 24.29\,\% and dropping the memory subsystem's combined share
from 36.4\,\% to 19.9\,\%. The architectural consequence is that
fully-digital threshold-logic compute is the dominant active power
bucket in the system that demonstrates it --- not a small auxiliary
block, but the largest active-datapath consumer at SoC scale.
Threshold logic in standard cells is therefore not merely viable
but load-bearing: it carries the system.

\subsection{Energy and throughput}
\label{sec:eval:energy}

At the same \texttt{max\_ff} corner, the SoC executes 17\,068 cycles
per image, i.e.\ 170.7\,\textmu s and \textbf{8.06\,\textmu J per
  inference} at 100\,MHz. This corresponds to \textbf{124\,113
  inferences/J} and a sustained throughput of 5\,860\,inf/s under
continuous polling. The companion \texttt{nom\_tt\_025C\_1v80} corner
is slightly more efficient at an estimated 7.09\,\textmu J/image and
141\,044\,inf/J. Compared with the Phase~3 7\,\(\times\)\,7
deployment on the same SoC, the grouped 14\,\(\times\)\,14 network
gains $+14.48$\,pp of accuracy at $+1.0\,\%$ energy overhead per
image: the post-P\&R netlist, sign-off artefacts, and clock are
byte-identical, and only the firmware DMEM layout (8 vs 2 words/image)
and the binary weight image change between the two deployments. The
accuracy-per-microjoule figure of merit therefore rises from
8.91\,pp/\textmu J to 10.62\,pp/\textmu J, a 19.2\,\% improvement at
unchanged silicon.

\subsection{Comparison with prior digital BNN ASICs}
\label{sec:eval:comparison}

Table~\ref{tab:comparison} positions the proposed SoC against
representative digital BNN ASICs. The aim of the comparison is not to
overtake the leading raw-throughput-per-watt baselines:
XNE~\cite{xne2018}, YodaNN~\cite{yodann2018}, and
ChewBaccaNN~\cite{chewbaccann2021} are 22--65\,nm commercial-flow
accelerators, and at those nodes a BNN datapath enjoys node-and-voltage
advantages that no sky130A 1.8\,V design can close. The contribution
of the present work sits in a different category, defined by two
simultaneous constraints: a fully open RTL-to-GDSII pipeline
(LibreLane + sky130A + OpenRAM) and a fully-digital TLG decomposition
mapped into the standard-cell library, with a soft RV32I core
orchestrating end-to-end inference at the SoC level.

The primary architectural comparator is TULIP~\cite{tulip}. TULIP
shares the threshold-logic philosophy but commits to a mixed-signal
flow with programmable flash devices for the threshold compare,
which ties the design to a specific PDK family. The present design
demonstrates that the same TLG mapping can be carried entirely by
digital standard cells, preserving portability across any digital
PDK. More broadly, prior threshold-logic accelerators across the
current-mode~\cite{beiu2003differential} and
memristor-based~\cite{papandroulidakis2019practical} approaches
share the same PDK-binding constraint: each is tied to a specific
fabrication technology by its choice of threshold-compare
primitive. TINKER demonstrates that this constraint is unnecessary
at the architecture level. The secondary comparator is
OpenSpike~\cite{openspike}, which shares the SoC-scale
neural-inference-on-sky130 substrate (PicoRV32 + OpenRAM) but
targets a spiking rather than a binary network, occupying a
complementary computational regime.
The figures reported in
Table~\ref{tab:comparison} are end-to-end at the SoC level --- CPU,
memories, clock tree, and tile included --- which is conservative
against the IP-block measurements that several baselines report and
makes the per-inference numbers directly comparable for a
system-integration audience.
\input{tables/table2_comparison}

% =====================================================================
% VI. Discussion — Target: ~300 words / 0.5 page
% ---------------------------------------------------------------------
% Three short paragraphs:
%   (a) what the tile-fraction jump means for follow-on work — once
%       memory is no longer dominant, tile-side optimization becomes
%       worth doing;
%   (b) honest limits — TT-only macro Liberty (one sentence), grouped
%       14x14 connectivity gap (one sentence), single-core soft CPU
%       leaves throughput on the table;
%   (c) what would change the picture — multi-corner Liberty for
%       OpenRAM, larger tile (e.g., 4 tiles in parallel for full
%       28x28), tape-out.
% =====================================================================
\section{Discussion}
\label{sec:discussion}

The most consequential finding is the change in where SoC power is
spent. With the register-file IMEM/DMEM of the Phase~3 baseline, the
TLG tile contributed 11.47\,mW of an SoC total of 106.06\,mW
(10.81\,\%) and the memory subsystem alone consumed 36.4\,\% of the
budget. Substituting OpenRAM macros leaves the tile bucket
byte-identical at 11.47\,mW but drops the system total to 47.21\,mW,
lifting the tile to 24.29\,\% and shrinking the memory share to
19.9\,\%. The system-integration consequence is that BNN compute is
no longer hidden by storage overhead at SoC scale, and tile-level
optimisation becomes a first-order target rather than a rounding
error against IMEM/DMEM dominance.

The reported per-inference energy of 8.06\,\textmu J is orders of
magnitude above what advanced-node commercial-flow accelerators
report, and the comparison in Section~\ref{sec:eval:comparison}
does not chase that gap. The architectural claim is independent
of the energy regime: that fully-digital threshold-logic
decomposition is a viable design point for inference accelerators,
demonstrated end-to-end at SoC scale. The decomposition ports
unmodified to any digital PDK; the architectural contribution
carries forward to nodes where energy efficiency becomes the
bottleneck.

The reported numbers are conservative on two methodological axes.
The OpenRAM macros ship Liberty for the TT\_1p8V\_25C corner only, so
all multi-corner STA passes use that single macro Liberty; the
slow-corner setup violations at \texttt{nom\_ss\_100C\_1v60} are a
documented LibreLane 3.x limitation rather than silicon defects, and
the macro-internal-power figure does not track corner. The headline
power number is taken at \texttt{max\_ff\_n40C\_1v95} and is
activity-aware (chip-pin VCD plus interior named-net annotations),
which is the conservative direction for an inference workload. The
present design does not demonstrate a multi-frequency power sweep, a
larger network beyond grouped 14\,\(\times\)\,14, or a tape-out;
those extensions are out of scope here rather than failures of the
approach.

% =====================================================================
% VII. Conclusion — Target: ~100 words / 0.15 page
% ---------------------------------------------------------------------
% Restate the headline metrics and the one structural finding the
% reader should remember (tile became 24.3% of system power once
% OpenRAM removed the regfile-IMEM dominance).
% =====================================================================
\section{Conclusion}
\label{sec:conclusion}

We have presented TINKER, a configurable threshold-logic-mapped
BNN inference SoC. The XNOR-popcount-threshold core is decomposed
into a chunked LUT scheme that lowers entirely onto
\texttt{sky130\_fd\_sc\_hd} standard cells, integrated as a
sixteen-neuron tile alongside a PicoRV32 RV32I core, a Wishbone B4
fabric, and OpenRAM IMEM/DMEM macros. An entire grouped binary MLP
maps onto the tile RTL through five software-scheduled sequential
evaluations, reaching 85.60\,\% MNIST accuracy at 8.06\,\textmu J
per inference and 47.21\,mW activity-aware power. The system-level
power decomposition exposes the TLG tile as the dominant active
bucket at 24.3\,\%, redirecting the optimisation target from the
memory subsystem onto the inference datapath itself. Tile-level
optimisation, larger networks, and tape-out become the natural
next steps.

% =====================================================================
% Acknowledgment — Target: 1--2 sentences if any
% =====================================================================
% \section*{Acknowledgment}
% TODO(stage 7): optional acknowledgment.

% ---------------------------------------------------------------------
% Bibliography
% ---------------------------------------------------------------------
\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
